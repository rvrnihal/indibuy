# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=paymentdb

# Security
APP_ENV=development
CSRF_TOKEN_LENGTH=32

# Payment Processing (Do NOT store sensitive card data - use payment gateway tokens)
# Use a third-party payment gateway like Stripe, PayPal, or Razorpay instead
